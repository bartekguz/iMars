import React from 'react';

const Navigation = () => (
    <nav style={{display: 'flex', 'justify-content': 'flex-end'}}>
        <p className="f3 link b dim white pa4 mr3 pointer">LOGIN</p>
    </nav>
);

export default Navigation;
