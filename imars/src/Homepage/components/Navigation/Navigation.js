import React from 'react';

const Navigation = ({ }) => (
    <nav style={{display: 'flex', justifyContent: 'flex-end'}}>
        <p className="f3 link b dim white pa3 mr4 pointer ttu tracked">sign out</p>
    </nav>
);

export default Navigation;
